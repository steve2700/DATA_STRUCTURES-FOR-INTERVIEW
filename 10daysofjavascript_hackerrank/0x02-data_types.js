/**
 * Variables named first integer,firstdecimal , and first string  are declared for you in the editor below. You must use the  operator to perform the following sequence of operations:
 * Convert SecondInteger   to an integer (Number type), then sum it with  and print the result on a new line using console.log.
 * Convert Secondfloat  to a floating-point number (Number type), then sum it with  and print the result on a new line using console.log.
 * Print the concatenation of FirstString and SecondString on a new line using console.log. Note that  must be printed first.

 * When we sum  the integers 4 and 12 , we get the integer 16 .
 * When we sum the floating-point 4.0numbers4.32  and , we get 8.32 . 
 * When we concatenate HackerRank with is the best place to learn and practice coding!, we get HackerRank is the best place to learn and practice coding!
 */

function performOperation(SecondInteger, Secondfloat, SecondString) {


// declaring our firstInteger and assign a value
const firstinteger = 4;
// declaring our firstdecimal and assign a value
const firstdecimal = 4.0;
// declaring our first string and assign a value
const FirstString = 'Hackerrank';

// convert the second integer to an integer number type then sum it up with first integer
console.log(firstinteger + parseInt(Secondinteger));

// convert the Secondfloat to a floating point number type and sum it up
console.log(firstdecimal + parsefloat(Secondfloat));

//convert the firstString and SecondString 
console.log(FirstString + SecondString);
}



